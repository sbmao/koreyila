{% if settings.enable_lazyload %}data-{% endif %}srcset="
{%- if preview_img.width >= 360 -%}
	{%- if preview_img.width >= 360 -%}
	    {{ preview_img | img_url: '360x' }} 360w
	{%- endif -%}
	{%- if preview_img.width >= 540 -%}
	    ,{{ preview_img | img_url: '540x' }} 540w
	{%- endif -%}
	{%- if preview_img.width >= 720 -%}
	    ,{{ preview_img | img_url: '720x' }} 720w
	{%- endif -%}
	{%- if preview_img.width >= 900 -%}
	    ,{{ preview_img | img_url: '900x' }} 900w
	{%- endif -%}
{%- else -%}
	{{ preview_img | img_url: 'master' }}
{%- endif -%}" {% unless settings.enable_lazyload %}src="{{ preview_img | img_url: '540x' }}"{% endunless %} sizes="(min-width: 1100px) {{preview_img.width | append: 'px'}}, (min-width: 750px) calc((100vw - 130px) / 2), calc((100vw - 50px) / 2)" alt="{{ preview_img.alt | escape }}" size="{{ preview_img.width }}" {% unless enable_rtl %}loading="lazy"{% endunless %} class="{% if class %}motion-reduce{% endif %}{% if settings.enable_lazyload %} lazyload{% endif %}"